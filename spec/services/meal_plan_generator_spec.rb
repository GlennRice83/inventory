require "spec_helper"

describe "MealPlanGenerator", pending: true do
  it "knows a user's recipe preferences"
  it "generates a schedule of meals to prepare"
  it "can guess at meals a user might like"
end
